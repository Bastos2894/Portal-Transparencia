paths:
  input: data/planilha_original
  output: data/planilha_tratada

filtro:
  entidade: "SECRETARIA DE ESTADO DE DESENVOLVIMENTO ECONOMICO"

excel:
  nome_saida: secretaria_desenvolvimento_economico.xlsx

# database:
#   tabela_fato: fato_despesas
#   tabelas_dimensoes:
#     tempo: dim_tempo
#     entidade: dim_entidade
#     fornecedor: dim_fornecedor
#     categoria: dim_categoria

pipeline:
  modo_carga: append   # append | replace | upsert
  chunk_size: 1000     # performance em grandes volumes

colunas:
  data: data
  entidade: entidade
  fornecedor: fornecedor
  categoria: categoria
  valor: valor
  descricao: descricao

Processamento:
  formatos_aceitos:
    - ".html"
    - ".xls"
    - ".xlsx"
    - ".csv"

  deletar_apos_processamento: true

  remover_duplicados: true

  coluna_chave: "numero_contrato" #usardo para duplicação